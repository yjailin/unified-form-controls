# Unified Form Controls

A WordPress plugin providing a token-based styling system for form controls on block themes.

This `main` branch is an empty starting point. The proof-of-concept implementation lives on the [`unified-form-controls-poc`](../../tree/unified-form-controls-poc) branch — see the open pull request for the proposed changes.
